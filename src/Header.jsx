@@ -4,7 +4,7 @@ import { add } from './Header.jsx';
 
 describe('add', () => {
   it('adds two positive numbers', () => {
-    expect(add(2, 3)).toBe(5);
+    expect(add(2, 4)).toBe(6);
   });
 
   it('adds negative and positive number', () => {